# S0基础迁移回滚说明

该迁移创建全新的认证与审计结构。测试和开发环境可以在确认无数据后按依赖关系逐表删除；
生产环境禁止直接删除，应先停止写入、导出 `users`、角色关系、会话和审计数据，再恢复迁移前
数据库备份。

推荐回滚顺序：

1. 停止Web和API。
2. 验证外部备份可用并记录恢复点。
3. 删除审计、会话、角色关联、权限、角色和用户表。
4. 删除 `UserStatus` 枚举。
5. 恢复上一版本镜像和数据库备份。

本文件是人工灾难恢复说明，不作为自动生产回滚脚本执行。
